test_no_validation:
    path: /no-validation
    defaults:
        _controller: app.tests.controller:testValidationAction

test_get_validation:
    path: /get-validation
    defaults:
        _controller: app.tests.controller:testValidationAction

test_post_validation:
    path: /post-validation
    defaults:
        _controller: app.tests.controller:testValidationAction

test_schema_with_inline_refs:
    path: /schema-with-inline-refs
    defaults:
        _controller: app.tests.controller:testValidationAction

test_schema_with_refs:
    path: /schema-with-refs
    defaults:
        _controller: app.tests.controller:testValidationAction
